= Этанол

[quote, народное творчество]
Седина в бороду, бес в ребро

Попытка на `vibe-кодить` открытый форк Camunda 8, и поправить чего мне там не нравится. Очень сильное влияние оказал проект: https://github.com/nitram509/lib-bpmn-engine

link:README_EN.adoc[English version]

== Статус

[cols="1,1,1"]
|===
| Компонент | Статус | Описание

| `pkg/bpmn_model`
| DONE
| BPMN 2.0 Model API — парсинг, билдер, Zeebe-расширения, DI, Query, валидация

| `pkg/bpmn_asserts`
| DONE
| Fluent test assertions для Zeebe — процессы, джобы, инциденты, сообщения, формы

| Stream Processing Engine
| В проектировании
| Движок обработки BPMN-процессов
|===

== Структура пакетов

----
rumunda/
├── pkg/
│   ├── bpmn_model/       # BPMN 2.0 Model API (парсинг + билдер)
│   └── bpmn_asserts/     # Test assertions для Zeebe процессов
├── docs/
│   ├── bpmn_model.adoc       # Документация BPMN Model API (RU)
│   ├── bpmn_model_en.adoc    # Документация BPMN Model API (EN)
│   └── stream_processing.adoc
├── go.mod
└── README.adoc
----

== Быстрый старт

=== BPMN Model: парсинг

[source,go]
----
import bpmn "github.com/esivres/c2h5oh/pkg/bpmn_model"

// Прочитать BPMN файл
bmi, err := bpmn.ReadFromFile("process.bpmn")

// Получить все service tasks
for _, task := range bpmn.GetTypedElements[bpmn.ServiceTask](bmi.ModelInstance) {
    fmt.Printf("Task: %s, type: %s\n", task.GetId(), task.GetName())
}
----

=== BPMN Model: создание процесса

[source,go]
----
import bpmn "github.com/esivres/c2h5oh/pkg/bpmn_model"

bmi := bpmn.CreateExecutableProcess("my-process").
    StartEvent("start").
    ServiceTask("call-api").Name("Call API").
        ZeebeJobType("api-worker").
        ZeebeInput("=requestData", "input").
        ZeebeOutput("=response", "result").
    EndEvent("end").
    Done()

// Сохранить в файл
bpmn.WriteToFile(bmi, "my-process.bpmn")
----

=== BPMN Model: AdHoc SubProcess (AI Agent)

[source,go]
----
bmi := bpmn.CreateExecutableProcess("ai-agent").
    StartEvent("start").
    AdHocSubProcess("agent").Name("AI Agent").
        ZeebeJobType("kamundarf:adhoc:v1").
        ZeebeAdHocConfig("toolCallResults",
            "={id: _meta.id, name: _meta.name, content: toolCallResult}").
        ZeebeInput("=userQuery", "userQuery").
    EndEvent("end").
    Done()
----

=== BPMN Assertions: тестирование процессов

[source,go]
----
import bpmn_assertions "github.com/esivres/c2h5oh/pkg/bpmn_asserts"

// Ожидание события
bpmn_assertions.Events().
    ProcessInstanceCompleted(processInstanceKey).
    WaitOn(s.RecordStream, 30*time.Second)

// Fluent assertions
bpmn_assertions.ForProcessInstance(s.T(), s.RecordStream, processInstanceKey).
    WithContext(ctx).
    IsCompleted().
    HasPassedElement("serviceTask1").
    HasPassedElementsInOrder("start", "serviceTask1", "end").
    HasNoIncidents().
    HasVariableWithValue("result", "expected")
----

== Документация

* link:docs/bpmn_model.adoc[BPMN Model API (RU)] — полная документация с примерами билдера, Query API, валидации
* link:docs/bpmn_model_en.adoc[BPMN Model API (EN)] — English version
* link:docs/stream_processing.adoc[Stream Processing] — архитектура движка (в разработке)

== Зависимости

|===
| Библиотека | Назначение

| `github.com/beevik/etree` | XML DOM (замена org.w3c.dom из Java)
| `github.com/stretchr/testify` | Test assertions
|===

== Ключевые решения

|===
| Java (Camunda/Zeebe) | Go (Rumunda)

| Interface + Impl class | Interface + struct embedding
| Abstract generic builders | Struct composition + Go generics
| DOM (org.w3c.dom) | etree (github.com/beevik/etree)
| Generics `<T extends X>` | Go generics `[T X]`
| Exceptions | error return values
| Maven multi-module | Single Go module
|===

== Источники и благодарности

* https://github.com/camunda/camunda[Camunda 8 / Zeebe] — оригинальный движок
* https://github.com/nitram509/lib-bpmn-engine[lib-bpmn-engine] — Go BPMN engine, вдохновивший проект
* Java bpmn-model и xml-model из Zeebe — основа для `pkg/bpmn_model`


